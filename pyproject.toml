[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dragon-lang"
version = "0.1.0"
description = "Dragon language MVP CLI"
requires-python = ">=3.10"

[project.scripts]
dragon = "dragonc:main"
